<?xml version="1.0" encoding="utf-8"?>
<!--
    Permissions used by the automation-engine monitors (ConnectivityMonitor,
    LocationMonitor). They are merged into the final app manifest and declared
    here so lint can validate the permission-protected API calls in this module.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <!-- Genuine 5G detection (NETWORK_MODE trigger): ServiceState NR
         registration needs the phone-state read. READ_BASIC_PHONE_STATE
         (API 33+) is auto-granted; READ_PHONE_STATE covers older versions.
         Mirrors the app manifest so lint validates the calls here. -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_BASIC_PHONE_STATE" />
    <!-- Pre-ring call screening (INCOMING_CALL trigger) and runtime CALL_BLOCK. -->
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />
    <!-- Caller category for INCOMING_CALL rules (CONTACT vs UNKNOWN). -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application>
        <service
            android:name=".MonitoringService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            tools:targetApi="34">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="automation-monitoring" />
        </service>

        <service
            android:name=".NexaCallScreeningService"
            android:exported="true"
            android:permission="android.permission.BIND_SCREENING_SERVICE">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

        <service
            android:name=".NotificationListener"
            android:exported="false"
            android:label="@string/notification_listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>

</manifest>
