<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- TetheringManager.registerTetheringEventCallback requires this normal
         permission. Declaring it in the owning library keeps static analysis
         and all consuming applications aligned with the public API contract. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
</manifest>
