<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--
        Android 17 (API 37): mandatory to reach LAN devices (smart hubs, NAS).
        Requested as a runtime permission for the HTTP request action.
    -->
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    <uses-permission android:name="android.permission.READ_LOGS" />
    <!--
        Phone-state read for genuine 5G detection in the NETWORK_MODE trigger:
        the legacy getNetworkType() reports LTE on NSA (LTE-anchored) networks,
        so the ServiceState NR registration is read instead. READ_BASIC_PHONE_STATE
        (API 33+) is auto-granted and covers getServiceState(); READ_PHONE_STATE
        covers older versions.
    -->
    <uses-permission
        android:name="android.permission.READ_PHONE_STATE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_BASIC_PHONE_STATE" />
    <uses-permission android:name="android.permission.MODIFY_PHONE_STATE" />
    <uses-permission android:name="android.permission.STATUS_BAR" />
    <uses-permission android:name="android.permission.EXPAND_STATUS_BAR" />
    <uses-permission android:name="android.permission.FORCE_STOP_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" tools:targetApi="s" />
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
    <uses-permission android:name="com.rikka.shizuku.permission.API" />

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.flash"
        android:required="false" />

    <!--
        Android 16+ (API 36+) Safer Intents hardening: an incoming intent from
        another app must match the target component's intent filter to be
        delivered. Every exported component below declares the matching filters
        (SmsReceiver/SMS_RECEIVED, SmsConsentReceiver/SMS_CONSENT_REQUEST,
        widgets/APPWIDGET_UPDATE, deep links/VIEW). Same-app PendingIntents
        (alarms, notification buttons, widget toggles) are explicitly exempt
        by the platform, so none of our internal routing is affected.
    -->
    <application
        android:name=".NexaFlowApplication"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:intentMatchingFlags="enforceIntentFilter"
        tools:targetApi="36"
        android:localeConfig="@xml/locales_config"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NexaFlow">
        <!-- Google Maps API key for the embedded map picker; empty by default
             (NEXAFLOW_MAPS_API_KEY gradle property). Without a key the picker
             shows a setup hint instead of a map. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.NexaFlow.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
                Deep link (P2-5): nexaflow://run-task/{automationId} runs the
                task directly, e.g. from a home-screen shortcut or Tasker.
                Handled in MainActivity (singleTop + onNewIntent).
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="nexaflow"
                    android:host="run-task"
                    android:pathPrefix="/" />
            </intent-filter>
        </activity>

        <receiver
            android:name="com.nexaflow.core.engine.AutomationAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.nexaflow.core.engine.action.RUN_AUTOMATION" />
                <action android:name="com.nexaflow.core.engine.action.START_MONITORING" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
                <!--
                    Re-schedule time triggers when the clock or time zone
                    changes (travel, manual clock edits, DST transitions): RTC
                    alarms are not shifted automatically, so without these the
                    next fire time drifts by the offset.
                -->
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.TIMEZONE_OFFSET_CHANGED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.nexaflow.core.engine.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!--
            Android 17 (API 37) SMS User Consent path: Play services delivers
            the user-approved message here instantly, without READ_SMS. This
            receiver is exported because Play services sends the broadcast.
        -->
        <receiver
            android:name="com.nexaflow.core.engine.SmsConsentReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="com.google.android.gms.auth.api.phone.SMS_CONSENT_REQUEST" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.nexaflow.core.execution.ReminderAlarmReceiver"
            android:exported="false" />

        <!--
            Dismiss action buttons on reminders/battery alerts: a button's
            PendingIntent broadcasts DISMISS_NOTIFICATION with the notification
            id; this plain receiver cancels it (no engine needed).
        -->
        <receiver
            android:name="com.nexaflow.core.execution.NotificationDismissReceiver"
            android:exported="false" />

        <!--
            Interactive notification action buttons: a button's PendingIntent
            broadcasts RUN_TASK_FROM_NOTIFICATION, which this receiver routes
            to the execution engine. Exported=false because only our own
            PendingIntents (created in NotificationActionButtons) target it.
        -->
        <receiver
            android:name="com.nexaflow.core.engine.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.nexaflow.core.execution.action.RUN_TASK_FROM_NOTIFICATION" />
                <action android:name="com.nexaflow.core.execution.action.REVERT_TASK_FROM_NOTIFICATION" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".NexaFlowToggleWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.nexaflow.app.action.TOGGLE_ALL" />
                <action android:name="com.nexaflow.core.execution.action.AUTOMATIONS_CHANGED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/toggle_widget_info" />
        </receiver>

        <receiver
            android:name=".NexaFlowStatusWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="com.nexaflow.app.action.REFRESH" />
                <action android:name="com.nexaflow.core.execution.action.AUTOMATIONS_CHANGED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/status_widget_info" />
        </receiver>

        <service
            android:name="com.nexaflow.core.engine.AppTriggerAccessibilityService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/app_trigger_accessibility_service" />
        </service>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!--
            WorkManager initializes on demand through NexaFlowApplication's
            Configuration.Provider (HiltWorkerFactory). The default
            androidx.startup initializer must be removed, or WorkManager would
            be created twice with incompatible configurations.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                tools:node="remove" />
        </provider>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!--
            Target of the «Save locally» row inside the backup share sheet.
            Exported so the system share sheet can start it; it shows no UI
            and just opens the SAF picker to write the cached backup.
        -->
        <activity
            android:name=".SaveBackupActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:label="@string/save_locally_label"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:noHistory="true">
            <!--
                A real share target: alongside the pinned initial intent, this
                intent-filter makes «Save locally» appear in the share sheet's
                regular app grid whenever a JSON backup is shared.
            -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/json" />
            </intent-filter>
        </activity>

        <!--
            WorkManager is configured through NexaFlowApplication's
            Configuration.Provider (so @HiltWorker MaintenanceWorker gets its
            Hilt-injected constructor). The default androidx.startup initializer
            would otherwise initialize WorkManager with the plain WorkerFactory
            first and make every @HiltWorker fail to construct at runtime —
            remove it here so only our configuration is used.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!--
            THE OPEN-CRASH FIX: sentry-android-core registers SentryInitProvider
            (and SentryPerformanceProvider) for automatic initialization on every
            app start. Sentry throws "DSN is required" and force-closes the app
            when no NEXAFLOW_SENTRY_DSN was baked into the build (CI releases),
            before Application.onCreate even runs. Sentry is opted in manually
            (and DSN-guarded) by SentryReporter, so auto-init is removed here —
            the app always opens, and crash reporting still works on builds that
            carry a DSN when the user enables it in Settings > Privacy.
        -->
        <provider
            android:name="io.sentry.android.core.SentryInitProvider"
            android:authorities="${applicationId}.SentryInitProvider"
            tools:node="remove" />
        <provider
            android:name="io.sentry.android.core.SentryPerformanceProvider"
            android:authorities="${applicationId}.SentryPerformanceProvider"
            tools:node="remove" />
    </application>

</manifest>
