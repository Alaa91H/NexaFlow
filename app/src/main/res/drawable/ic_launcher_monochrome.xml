<?xml version="1.0" encoding="utf-8"?>
<!--
    Monochrome launcher layer: checkmark as a single silhouette, tinted by
    the system with the user's wallpaper palette (Material You). Matches the
    foreground glyph exactly — no clock.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">

    <!-- Bold checkmark. -->
    <path
        android:fillColor="#00000000"
        android:strokeColor="#FFFFFFFF"
        android:strokeWidth="12"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:pathData="M33,54L46,67L75,38" />
</vector>
