<?xml version="1.0" encoding="utf-8"?>
<!--
    Launcher background: light pastel Material You gradient (soft indigo →
    violet). Uses a plain <shape> gradient drawable — no vector aapt:attr
    gradients — so every launcher/system icon loader renders it reliably.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="linear"
        android:angle="135"
        android:startColor="#FFC9D4FF"
        android:centerColor="#FFE0C9FF"
        android:endColor="#FFF5EDFF" />
</shape>
