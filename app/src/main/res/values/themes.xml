<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.NexaFlow" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">?android:attr/colorBackground</item>
    </style>

    <!-- Branded splash (Android 12+ via core-splashscreen, backported below):
         solid brand background + the adaptive launcher icon centered, exactly
         the Google 2026 pattern (Calendar/Gmail open on their brand color). -->
    <style name="Theme.NexaFlow.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/Theme.NexaFlow</item>
    </style>
</resources>
